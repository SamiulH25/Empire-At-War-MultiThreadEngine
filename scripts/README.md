# Research artifacts

Game install files live in Steam's directory, not here. This folder holds analysis
tooling (Python scripts) as it gets written.

Scripts planned:
- `pe_analysis.py` — dump PE structure, imports, exports, strings
- `meg_extract.py` — .meg archive reader/extractor (once format is confirmed)
- `string_scan.py` — targeted string search across binaries
